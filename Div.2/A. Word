#include <bits/stdc++.h>
using namespace std;
int main ()
{
    char c[100];
    int a,b=0;
    cin>>c;
    a=strlen(c);
    for(int i=0;i<a;i++)
    {
        if(c[i]<'a')
            b++;
    }
    printf("%s",(b>a/2)?strupr(c) : strlwr(c));
    return 0;
}
